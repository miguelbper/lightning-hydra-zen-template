# @package _global_

# See https://hydra.cc/docs/patterns/configuring_experiments/ for how to configure an experiment

defaults:
  - override /datamodule: mnist
  - override /model: mnist

experiment_name: mnist
evaluate: true
ckpt_path: null
seed: 42
monitor: val/MulticlassAccuracy
mode: max
